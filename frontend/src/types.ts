export interface Poster {
  id: number;
  title: string;
  description: string;
  imageUrl?: string;
  createdAt: Date;
}

export interface Project {
  id: number;
  title: string;
  description: string;
  imageUrl?: string;
  createdAt: Date;
}